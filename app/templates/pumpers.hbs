<div class="row">
    <div class="col-md-3">
      <div class="list-group">
        {{#each pumper in model}}
          {{#link-to 'pumper' pumper classNames='list-group-item'}}{{pumper.name}} {{pumper.lastName}}{{/link-to}}
        {{/each}}
      </div>
    </div>
</div>
